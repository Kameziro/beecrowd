#include <stdio.h>
 
int main() {
 
 int A;
 int B;
 float C;
 float salario;
 
 scanf("%d", &A);
 scanf("%d", &B);
 scanf("%f", &C);
 
 salario = B*C;
 
 printf("NUMBER = %d\n",A);
 printf("SALARY = U$ %.2f\n", salario);
 
    return 0;
}
